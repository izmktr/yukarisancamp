{
  "name": "web-app-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-types": "npx json-schema-to-typescript --input ../schema/userProfile.schema.json --output src/types.ts",
    "build": "npm run generate-types && echo 'Build completed'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-schema-to-typescript": "^14.0.5"
  }
}
