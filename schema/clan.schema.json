{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Clan",
  "description": "A clan data structure for Discord bot.",
  "type": "object",
  "properties": {
    "source": {
      "description": "The source of the clan data, either from Discord or web.",
      "type": "string",
      "enum": ["discord", "web"]
    },
    "clanid": {
      "description": "The unique ID of the clan.",
      "type": "string"
    },
    "name": {
      "description": "The name of the clan.",
      "type": "string"
    },
    "bosslaps":{
      "description": "The number of laps for the clan battle boss.",
      "type": "array",
      "items": {
        "type": "integer"
      },
      "minItems": 5,
      "maxItems": 5
    },
    "createdAt": {
      "description": "The timestamp when the clan was created.",
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["source", "clanid", "name", "bosslaps", "createdAt"]
}