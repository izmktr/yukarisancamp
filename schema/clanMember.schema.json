{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ClanMember",
  "description": "A clan member data structure for Discord bot.",
  "type": "object",
  "properties": {
    "id": {
      "description": "The unique ID of the user.",
      "type": "integer"
    },
    "name": {
      "description": "The user's name.",
      "type": "string"
    },
    "mention": {
      "description": "The mention name for the user.",
      "type": "string"
    },
    "taskkill": {
      "description": "The number of task kills performed.",
      "type": "integer"
    },
    "history": {
      "description": "The attack history list.",
      "type": "array",
      "items": {
        "$ref": "attackHistory.schema.json"
      }
    },
    "plan": {
      "description": "The attack plan list.",
      "type": "array",
      "items": {
        "type": "integer"
      }
    },
    "attacktime": {
      "description": "Attack management flags (None: not attacked, number: overtime seconds).",
      "type": "array",
      "items": {
        "type": ["integer", "null"]
      }
    },
    "sortie": {
      "description": "Attack in progress flag (-1: not attacking, other: sortie number).",
      "type": "integer"
    },
    "boss": {
      "description": "The boss being attacked.",
      "type": "integer"
    },
    "attackmessage": {
      "description": "The attack declaration message (Discord Message object).",
      "type": ["string", "null"]
    },
    "reportlimit": {
      "description": "The deadline for reminders.",
      "type": ["string", "null"],
      "format": "date-time"
    },
    "lastactive": {
      "description": "The timestamp of the last activity.",
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["id", "name", "mention", "taskkill", "history", "plan", "attacktime", "sortie", "boss", "lastactive"]
}